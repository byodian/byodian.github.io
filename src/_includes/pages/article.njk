---
layout: "base"
---

<div id="root" class="container max-w-4xl mx-auto px-6 pt-4">
  <header class="mt-16 mb-8">
    <p><a class="font-bold" href="/">byodian's blog</a></p>
  </header>

  <div class="pb-5 mb-5 border-b  border-gray-100">
    <h1 class="text-5xl">{{ title }}</h1>
    <p class="text-gray-400">
      <time datetime="{{ created | dateReadable }}">发布于{{ created | dateReadable }}</time>

      {% if updated %}
       · <time datetime="{{ updated | dateReadable }}">更新于{{ updated | dateReadable }}</time>
      {% endif %}
    </p>
  </div>

  <article class="prose lg:prose-lg">
    {{ content | safe }}
  </article>

  {% include "layouts/footer.njk" %}
</div>
