{% extends "layouts/base.njk" %}

{% block content %}

  <section class="bookmarks">
    <section class="categories">
      {% for bookmark in collections.bookmarks %}

        <article class="article category__item">
          <div class="category__content">
            <h3 class="article__heading">
              <a class="article__link" href="{{ bookmark.url }}">
                {{ bookmark.data.title }}
              </a>
            </h3>
            <div class="article__info">
              <time class="article__time" datetimme="{{ bookmarkdate | dateISO}}">{{ bookmark.date | dateReadable }}</time>
              <a href="#" class="stress article__tag">{{ bookmark.data.category }}</a>
            </div>
            <p class="article__overview">{{ bookmark.data.description }}</p>

          </div>
          <a class="bookmarks__ref" href="{{ bookmark.data.sourceUrl }}">{{ bookmark.data.sourceName }}</a>
        </article>
      {% endfor %}

      {# <div class="btn-box">
        <button class="btn btn-prev">
          <a href="#">
            <span>&larr;
            </span>上一页</a>
        </button>
        <button class="btn btn-next">
          <a href="#">下一页
            <span>&rarr;</span></a> 
        </button>
      </div> #}

    </section>
    <aside class="sidebar">
      <section class="tags">
        <h3 class="heading-tertiary heading-tertiary--aside">标签</h3>
        <ul class="tags__items">

          {% for bookmarkTag in categories.bookmarkTags %}
            <li class="tags__item">
              <a class="tags__link" href="{{ bookmarkTag.url }}">{{ bookmarkTag.name }}</a>
            </li>
          {% endfor %}

        </ul>
      </section>
      <section class="sidebar__about">
        <h3 class="heading-tertiary heading-tertiary--aside">About</h3>
        <div class="sidebar__about-info">
          <figure class="sidebar__photo-box">
            <img class="sidebar__photo" src="{{ setting.avatar }}" alt="avatar photo">
          </figure>
          <p class="sidebar__para">{{ setting.personalInfo }}</p>
        </div>
      </section>
      <figure class="social-icons sidebar__social">
        {% for icon in setting.socials %}
          <a class="social-link" href="{{ icon.url}}">
            <svg class="social-icon">
              <use xlink:href="/assets/img/sprites.svg#{{ icon.name }}"></use>
            </svg>
          </a>
        {% endfor %}
      </figure>
    </aside>
  </section>
{% endblock content %}
{% block pageFooter %}{% endblock pageFooter %}